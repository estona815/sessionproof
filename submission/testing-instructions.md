# Judge testing instructions

Public URL: `[PRODUCTION URL REQUIRED]`

1. Open the URL in an incognito browser; no account is required for the fictional demo.
2. Select **Run Guided Demo**, then **Run Credit Agent**.
3. Confirm the UI labels the run `Fixture demo` and `No Gemini call`.
4. Open **Composition split totals 110%** and inspect file, line, quote, confidence, and rule.
5. Review the contributor ledger, alias/missing-vocalist/title/sample items, and 18/100 deterministic policy.
6. Record Lena's fictional acknowledgement and observe the recomputed workflow message.
7. Download CSV, JSON, evidence, Markdown, and PDF outputs.
8. Visit `/operator` and confirm real business metrics are separate zeros unless verified evidence has been added.
9. Visit `/trust` and `/api/health`.
10. For the production Gemini path, use the supplied judge project route/credentials `[REQUIRED]` and verify a redacted run showing Vertex/Gemini model, usage, schema-valid claims, exact-quote rejection, and no raw source logging.

Local alternative: Node 20.9+, `pnpm install --frozen-lockfile`, `pnpm dev`. Validation commands are documented in README.
