# Evaluation plan

The fictional fixture defines expected claim IDs, conflict rule IDs, evidence links, 110% math, and an 18/100 replay result. Unit/integration/security/browser tests cover fragmentation, hashes, normalization, basis points, conflicts, questions, confirmation primitives, changes, exports, prompt isolation, PII redaction, file rules, financial totals, and UI workflow.

Before submission, add a consented, de-identified evaluation set and report precision/recall for contributor, role, percentage, evidence-link validity, unsupported-claim rate, conflict precision/recall, false merges, runtime, model failure, tokens, cost, exports, and security tests. Report sample size and confidence limitations. Fixture scores must never be presented as real-customer performance.
